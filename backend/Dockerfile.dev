# Imagem de desenvolvimento com Maven — fonte montada via volume no docker-compose
FROM maven:3.9-eclipse-temurin-21
WORKDIR /app
EXPOSE 8080
CMD ["mvn", "spring-boot:run", "-Dspring-boot.run.profiles=local"]
