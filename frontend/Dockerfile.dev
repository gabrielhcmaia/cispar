# Imagem de desenvolvimento — fonte e node_modules gerenciados por volumes no docker-compose
FROM node:20-alpine
WORKDIR /app
COPY package.json yarn.lock* ./
RUN yarn install
EXPOSE 5173
CMD ["yarn", "dev", "--host"]
